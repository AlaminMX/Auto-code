# Server-side only — set these in Vercel's Project Settings → Environment Variables.
# Never prefix with NEXT_PUBLIC_, or it gets shipped to the browser.

# Free Gemini API key from https://aistudio.google.com/app/apikey
# gemini-2.5-flash free tier: 1,500 requests/day, 1M token context, no card required.
GEMINI_API_KEY=AIza...
